# Assignment content

Each assignment directory contains one shared `brief.md`. The same brief is shown to students and instructors.

Use `<!-- slide -->` to create a new screen. Add instructor-only guidance with a hidden `` block on the relevant slide; Icerynk shows it only in the instructor control plane.
