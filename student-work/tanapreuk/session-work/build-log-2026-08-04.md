# Build log — 2026-08-04 (Session 4)

*Draft prepared by my agent (Hermes, model: deepseek-v4-flash) for my review before commit.*

## 1. What did I try today?

Set up the Session 4 toolchain: authenticated the GitHub CLI, forked the course
repository to my account, cloned it, ran `scripts/init-student.mjs`, created my
work branch `work/tanapreuk/session-04-agent-check`, and made my first commit
through my own agent — the "each of you makes one commit through your own agent"
proof from the capstone brief.

## 2. What surprised me or failed?

GitHub CLI's interactive login would not accept input through the background
terminal — the prompt needed a real terminal session (`TERM` was unset) and its
output file buffered until exit. We worked around it with the browser
device-code flow, which is also what the setup guide recommends. So:
one-time code → browser → authorized. No credentials were ever typed into the
agent.

## 3. What will I do differently next time?

- Start the sync ritual at the beginning of every class (`git fetch upstream`,
  `git merge --ff-only upstream/main`) so my fork never drifts.
- Keep all work on `work/<login>/<task>` branches, never on `main`.
- Use the course agent check file before letting the agent edit anything.

## AI use

- **Tools/models:** Hermes agent running deepseek-v4-flash; GitHub CLI and the
  course's own `init-student.mjs` / `validate-local-work.mjs` /
  `commit-work.mjs` scripts.
- **Where the output helped:** locating and reading the course instructions
  (README, CONTRIBUTING, setup guide, AGENTS.md), picking the exact commands
  the professor specifies, and diagnosing the login hang.
- **Where I checked rather than trusted:** I reviewed the remotes, the branch
  name, and this file before approving anything; the agent never pushed or
  opened a pull request without asking.
